/*import React from 'react';

function FormSelect({ label, id, children, ...props }) {
  return (
    <div>
      <label htmlFor={id} className="block text-sm font-medium text-off-white/80 mb-1">
        {label}
      </label>
      <select
        id={id}
        {...props}
        // La clase 'accent-esmeralda' cambia el color del selector/resaltado en navegadores modernos.
        className="w-full p-3 bg-white text-grafito border border-gray-300 rounded-lg focus:ring-2 focus:ring-soft-gold focus:border-soft-gold transition accent-esmeralda"
      >
        {children}
      </select>
    </div>
  );
}

export default FormSelect;
*/
